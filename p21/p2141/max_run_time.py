import heapq
from typing import List


class Solution:
    def maxRunTime(self, n: int, batteries: List[int]) -> int:
        # heap = []
        # heapq.heapify([-battery for battery in batteries])
        # count = 0
        # while heap:
        #     next
        pass